[package]
name = "test-macro"
version = "0.1.0"
edition = "2021"

[lib]
proc-macro = true

[workspace]
# Empty workspace to prevent being part of rust workspace

[dependencies]
proc-macro2 = "1.0"
quote = "1.0"
syn = { version = "2.0", features = ["full"] }
